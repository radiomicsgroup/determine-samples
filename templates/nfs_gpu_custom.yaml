bind_mounts:
- container_path: /nfs
  host_path: /nfs
  propagation: rprivate
  read_only: false
environment:
  force_pull_image: false
  image:
    cuda: harbor.vhio.net/determine/custom_gpu:0.24.0.7